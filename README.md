# Read Me

Converted into plain HTML + Tailwind from Svelte 5

Copying and pasting bit by bit from the resume-2024 repo

JS elements will need to come later.

## Tailwind compilation

To have Tailwind compiled, use the following command

`npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch
